# EchoGist — v1.1 Engineering Plan (menu/UX overhaul)

**Status:** locked by `/plan-eng-review` 2026-06-17 (ENG CLEARED; cross-model outside voice absorbed)
**Architecture:** UX-layer replacement of the input/output seam behind a dependency-injected
`UI` Protocol. **Not** a pipeline rearchitecture — transcribe/summarize/render logic is frozen.
**Authority:** [CLAUDE.md](../CLAUDE.md) · builds on the locked v1 spec
[`docs/V1_ENGINEERING_PLAN.md`](./V1_ENGINEERING_PLAN.md) (stack/pipeline unchanged).
**Lineage:** office-hours design `~/.gstack/projects/echogist/pc-main-design-20260617-142906.md`
(APPROVED) → this plan. Decisions logged in the gstack decision store.

No implementation code in this phase. This document is the build spec.

---

## 1. Goal & scope (locked)

Replace the bare `input()` / numeric-prompt loop (~2/10 UX) with a modern arrow-key,
styled console using **questionary** (navigation/input) + **rich** (output/formatting),
targeting ~9/10. Six operator-facing surfaces change; nothing else does.

1. **Main menu** — `questionary.select`, arrow-key navigation, icons per item, under a
   `rich.Panel` "EchoGist" banner.
2. **Settings** — current settings as a colored `rich.Table`; edit via nested
   `questionary.select`; threshold via `questionary.text` with numeric `>= 0` validation.
3. **Transcription progress** (operator's #1 screen) — `rich.Progress` `%/elapsed/ETA` bar;
   spinners for the non-progress waits (provision, model load, summary call); ✓/✗ colored
   status lines replacing bare "GPU preflight OK" text.
4. **Paid-call confirm** — surfaces the local cost estimate; below threshold an
   acknowledgment that proceeds, above threshold an explicit gate defaulting to No.
   Must not weaken the killswitch or the `$0.50` threshold logic.
5. **Result / error** — final `rich.Panel` on success; rich-rendered errors, not tracebacks.
6. **Polish** — spinners on all wait stages; centralized theme.

**Cut / deferred:** see §9. No Textual / full-TUI framework. No plain-input fallback UI
(TD-7). No palette-as-data in `models.toml`.

---

## 2. Stack additions (locked)

| Concern | Choice | Note |
|---|---|---|
| Navigation / input | `questionary==2.*` (pulls `prompt_toolkit`, `wcwidth`) | arrow-key select, text, confirm; needs a TTY |
| Output / formatting | `rich` (lock-deps-resolved major — 13 or 14) | panels, tables, Progress, spinners; capability detection built-in |
| Windows color shim | `colorama ; sys_platform == 'win32'` (transitive) | marker-guarded by the universal resolver |

Both new packages are **pure-Python, no native/ABI component** — they do not interact with
the `ctranslate2 ↔ cuDNN` skew the v1 lockfile (§12.2) guards. The existing **`setuptools<81`**
pin (ctranslate2 imports `pkg_resources`) MUST survive the re-lock.

---

## 3. Architecture — the UI seam

The menu is already fully dependency-injected: every prompt goes through `Deps.reader`
(default `input`), every line through `Deps.log` (default `print`) — `menu.py:87-103`. The
CI killswitch invariant rides on this: tests pipe stdin and stub `log` to run the whole menu
with **no TTY, no key, no network**. questionary/prompt_toolkit **require a TTY** and break
under piped stdin. So the overhaul keeps the seam and routes I/O through a `UI` Protocol.

```
                         echogist/menu.py  (pure flows — logic unchanged)
                                   │ calls ui.select/text/confirm/info/success/
                                   │       warn/error/table/banner/progress/spinner
                                   ▼
                         ┌───────────────────────┐
            Deps.ui ───▶ │   UI  (Protocol)       │   ← typed seam
                         └───────────┬───────────┘
              ┌──────────────────────┴──────────────────────┐
              ▼                                              ▼
  RichQuestionaryUI (production)                  StubUI (tests only)
   rich.Console + questionary.Style                records (method, plain message)
   + theme glyphs; touches the TTY                 select/text/confirm pop a queued answer
   progress()/spinner() → rich-backed              progress()/spinner() → NO-OP handles
   handles (.advance_to / .done)                   (.advance_to = pass, .done → .messages)
              │                                              │
              └────────── both satisfy ProgressHandle / SpinnerHandle (Protocols) ──────────┘

KILLSWITCH: StubUI has no TTY, no rich, no network → the full menu runs in CI offline.
            Production RichQuestionaryUI imports rich/questionary (offline libs); anthropic
            stays lazy inside summarize. Import-time network-free invariant holds.
```

**Why handles are Protocol-typed (not rich-native yields).** `progress()`/`spinner()` return
`ProgressHandle`/`SpinnerHandle` (Protocols). Yielding rich-native `Progress`/`Status` objects
would force `StubUI` to fake a rich-compatible context manager, puncturing the no-TTY purity
the seam exists for. The stub returns a trivial no-op handle; swapping rich later touches only
the adapter. (Ratified at the eng-review Step 0 scope gate — `Choice` dropped, handles kept.)

**New source types:** `UI`, `RichQuestionaryUI`, `ProgressHandle`, `SpinnerHandle` (+ test-only
`StubUI`). `Choice` is NOT a new type — use a tuple / `questionary.Choice`.

---

## 4. Theme & Windows fallback (locked)

- **Theme module** `echogist/theme.py` — one `rich.Theme`, one `questionary.Style`, and the
  fancy/ASCII glyph tables, as the centralized source. NOT `models.toml`: a palette is
  developer taste, not operator behavior; TOML-serializing it would add a parse/validate
  (new `ConfigError`) surface for values the solo operator never tunes.
- **Capability detection** — read `rich.Console` capabilities (`encoding`, `legacy_windows`),
  set one `fancy` flag, swap a glyph table: fancy (`✓`, `✗`, `→`, Unicode box) vs ASCII
  (`OK`, `X`, `>`, ASCII box). Honor `NO_COLOR`. rich auto-degrades color/box-drawing but does
  NOT strip emoji placed in your own strings — the glyph table closes that gap on legacy cmd
  (cp437/cp1251). Correctness on the ship target, not decoration.

```
detect_caps(console) → fancy?  ── utf-8 + TTY + not legacy_windows + NO_COLOR unset ──▶ FANCY glyphs
                                └─ else (cp437/cp1251, NO_COLOR, legacy) ────────────▶ ASCII glyphs
```

---

## 5. Pipeline-adjacent seams (the only stage touches)

**Progress (`transcribe.py`).** Add `progress: Callable[[float], None] | None = None`.
`transcribe.py:152` already computes `total = info.duration`; `:159-160` already computes
`raw.end/total` per segment.

- `total > 0` → `progress(raw.end/total)` → `%/elapsed/ETA` bar.
- `total == 0` (zero-duration / unprobeable; TTY guaranteed by §6) → a determinate-by-work
  readout (segments processed + elapsed, **no %/ETA**) with an explicit ✓ at completion —
  honest no-ETA progress, never an infinite spinner. No div-by-zero.
- The per-segment `log` line at `:160` is **removed** (the bar replaces it). The pre-loop
  language+duration line at `:154` feeds the progress UI's **label/header** (known once,
  before the loop) — it does NOT remain a separate `log()` print alongside the bar.
- **Extract a pure `_collect_segments(stream, total, progress) -> tuple[Segment, ...]`** from
  the GPU adapter so the fraction math + `total==0` readout + empty stream are unit-testable
  with fake raw segments (no GPU). Matches the project's pure-stage boundary. Keep it scoped:
  the helper assembles the `Segment` tuple + emits progress, nothing more.

**Confirm (`cost.py:141-162`).** `confirm_proceed` keeps the threshold policy in `cost.py`;
swap `reader=` for a narrow `confirm: Callable[[str, bool], bool]` (prompt, default) so
`cost.py` stays decoupled from the full `UI` Protocol.

- **Below threshold** is an ACKNOWLEDGMENT, not a gate: show the cost via `ui.info` + a plain
  "press Enter to continue" beat that ALWAYS proceeds (faithful to today's unconditional
  proceed) — NOT a `confirm` Y/n widget (which would let `N` decline a cheap call that always
  ran).
- **Above threshold** → `confirm(default=False)`.
- Preserves the exact `$0.50`-threshold UX; killswitch-safe (local, no wire).

---

## 6. Never-crash / exit contract (locked)

The loop relies on `EOFError` == clean exit (`menu.py:375,388`) and "fail loud, return to
menu." questionary/prompt_toolkit do NOT raise `EOFError` on cancel (Ctrl-C returns `None` /
raises `KeyboardInterrupt`). The adapter MUST:

1. At construction, if stdin/stdout is **not a TTY**, print one clear line and exit cleanly
   BEFORE the menu loop (no prompt_toolkit traceback).
2. Translate questionary cancel (`None` / Ctrl-C / Ctrl-D) into the loop's clean-exit (raise
   `EOFError` or a sentinel the loop already exits on); catch `KeyboardInterrupt` so Ctrl-C
   never crashes. Keep `menu.py`'s two `except EOFError` arms live in production.
3. The outermost `except` in `run_menu` (`menu.py:391-394`) falls back to builtin `print` if
   `ui.error()` itself raises (a broken UI can't report its own failure through itself).

---

## 7. Tests (100% of new paths; killswitch stays green)

Dev gate unchanged: `bash scripts/dev-loop` (ruff + mypy + pytest) against the stub
summarizer — currently 225 passed / 2 skipped. The migrated suite must stay green.

| Area | Coverage | Tool |
|---|---|---|
| `theme.detect_caps` | utf-8 TTY → fancy; cp437/non-utf8 → ascii; `NO_COLOR` → ascii | unit `test_theme.py` |
| `theme.glyph` | fancy vs ascii table selection | unit |
| `transcribe._collect_segments` | fraction values; `total==0` readout; empty stream | unit `test_transcribe.py` |
| `RichQuestionaryUI` no-TTY guard | fake non-TTY → clean exit message | unit `test_ui.py` |
| Cancel → clean exit (T9) | StubUI simulates cancel/Ctrl-C → loop exits, no crash | unit `test_ui.py` |
| `cost.confirm_proceed` reshape | below-threshold acknowledge-proceeds; above-threshold default No | unit `test_cost.py` (assertion rewrite) |
| `menu.py` flows (REGRESSION) | every F1/F3/F6/F13 + settings + exit + source×action path stays green under `StubUI` | `test_menu.py` migration |

The live `RichQuestionaryUI` adapter is TTY-bound → **operator-verified on Windows**, no WSL
unit test (the dev loop is blind to `isatty`/`legacy_windows`/key-handling; only the
acceptance gate exercises them).

---

## 8. Implementation tasks (T1–T9)

P1 blocks ship; P2 lands same branch. Effort = human / CC-compressed.

- **T1 (P1, 40min / 12min)** — transcribe: extract pure `_collect_segments(stream,total,progress)`; `transcribe()` calls it. → `echogist/transcribe.py`, `tests/test_transcribe.py`
- **T2 (P1, 1.5d / 35min)** — ui: `UI` Protocol + `ProgressHandle`/`SpinnerHandle` Protocols + `RichQuestionaryUI` adapter + `StubUI`; `isatty()` fail-loud guard; print-fallback backstop. → `echogist/ui.py`, `tests/test_ui.py`
- **T3 (P1, 2h / 15min)** — theme: `theme.py` (rich.Theme + questionary.Style + glyph tables + `detect_caps`, honor `NO_COLOR`). → `echogist/theme.py`, `tests/test_theme.py`
- **T4 (P1, 1d / 30min)** — menu: rewire flows to `ui.*`; remove dead ANSI helpers (`_CYAN`/`_DIM`/`_RESET`/`_h()`/`_settings_text`). → `echogist/menu.py`
- **T5 (P1, 30min / 10min)** — cost: reshape `confirm_proceed` to a narrow `confirm` callable; keep threshold policy; below=acknowledge-proceed, above=default False. → `echogist/cost.py`, `tests/test_cost.py`
- **T6 (P1, 1h / 20min)** — tests: migrate `test_menu.py`→`StubUI` and `test_cost.py`→stub confirm; regression-protect every green path. → `tests/`
- **T7 (P1, 30min / 10min)** — deps: add `questionary==2.*` + `rich` to `requirements.in` PRESERVING `setuptools<81`; `scripts/lock-deps`; **review the lock diff for unexpected transitive bumps before committing**; Windows cold-run `--require-hashes` verify. → `requirements.in`, `requirements.lock`
- **T8 (P2, 20min / 8min)** — ui: `total==0` readout (segments+elapsed, no %/ETA, ✓); progress label carries the language+duration header. → `echogist/ui.py`, `tests/test_ui.py`
- **T9 (P1, 30min / 10min)** — ui: translate questionary cancel (None/Ctrl-C/Ctrl-D) → clean-exit; catch `KeyboardInterrupt`; keep `except EOFError` arms live. → `echogist/ui.py`, `tests/test_ui.py`

### Parallelization

```
Lane A: T7 deps        (independent — do first; deps must be importable)
Lane B: T3 theme       (independent)
Lane C: T1 transcribe  (independent — different module)
   └─ converge ─▶ T2 ui (needs T3+T7) ─▶ T5 cost ─▶ T4 menu ─▶ T8/T9 ─▶ T6 test migration
```
Conflict flag: T2, T8, T9 all touch `ui.py` → keep that chain sequential.

---

## 9. NOT in scope

- **Plain-input fallback UI** for piped/non-TTY runs — deferred ([TD-7](./TECHNICAL_DEBT.md)).
  A personal interactive tool never hits it; fail-loud (§6) is enough.
- **Palette-as-data in `models.toml`** — a palette is dev taste; theme module suffices.
  Localized later change if ever wanted.
- **Textual / full TUI framework** — questionary+rich is the local input-layer replacement.
- **§12.3 GPU smoke for this lock bump** — correctly skipped; the new deps are pure-Python,
  ABI-independent. Acceptance gate = Windows cold-run `--require-hashes` install + a render.
- **Pipeline logic changes** — transcribe/summarize/render outputs are byte-for-byte unchanged.

---

## 10. Acceptance

- `bash scripts/dev-loop` green (ruff + mypy + suite) under `StubUI` — no TTY/key/network.
- New unit tests (theme detection, `_collect_segments`, no-TTY guard, cancel→exit, confirm
  reshape) all present and passing.
- `requirements.lock` regenerated; lock diff reviewed; `--require-hashes` install verified on
  a Windows cold run.
- Operator confirms on Windows: arrow-key menu + settings; emoji/arrows render OR ASCII
  fallback kicks in; the `%/ETA` transcription bar advances; `total==0` readout completes with
  ✓; confirm defaults to No above threshold and proceeds below; Ctrl-C exits cleanly;
  result/error panels render. No transcribe/summarize/render output change.
