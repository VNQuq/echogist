# Secrets — NEVER commit
.env
.env.*
!.env.example
.envrc
secrets/
*.key
*.pem

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
.uv/
dist/
build/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# IDE / OS
.idea/
.vscode/
*.swp
*.swo
.DS_Store
Thumbs.db

# EchoGist runtime artifacts (recovery state, not source)
output/
# Downloaded Whisper model (TD-1) — fetched/pre-placed, never committed
models/
# Model-fetch download cache + partial downloads
.cache/
*.part
# Operator's runtime settings — written by the Settings menu, not source
config/settings.json

# Data / logs / dumps
data/
logs/
*.log
*.dump
*.sql.gz
*.tar.gz
.scratch/

# Node
node_modules/
.next/

# Docker volumes
pg_data/
redis_data/

# Backup artefacts
*.bak
*.backup
_commit_msg.txt

# gstack — local-only artifacts
.gstack/
.claude/
.context/
~/.gstack/
.gstack-worktrees/